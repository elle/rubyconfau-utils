!!! 5
%html{lang: 'en'}
  %head
    %title Ruby Conf Australia 2014 - Talk Proposals
    %meta{charset: "utf-8"}
    %meta{name: "author", content: "Ruby Australia"}
    %meta{name: "copyright", content: "Copyright Ruby Australia 2012-2013. All Rights reserved." }
    %script{src: "https://ajax.googleapis.com/ajax/libs/jquery/1.8.2/jquery.min.js"}
    :javascript
      $(function() {
        $('.proposal').hide();
        $('h2').on('click', function() {
          $(this).next().toggle();
        });
      });
    %style(type="text/css")
      body { font-family:sans-serif; color: #333; background: #efefef; }
      div.wrapper { max-width: 40em; }
      ul { list-style: none; margin: 0; padding: 0; }
      li { margin-top: 0.5em; }
      li h2.title { color: #777; font-size: 1.2em; font-weight: normal; border-bottom: 1px solid; }
      li .proposal { font-size: 14px; }
      li .proposal h2 { font-size: 16px; }
      img { width: 10em; }

  %body
    .wrapper
      %h1 Ruby Conf AU 2014 Talk Proposals
      %ul
        - CSV.foreach('anons.csv', headers: true) do |row|
          %li
            %h2.title= "#{row[0]}: #{row[1]}"
            .proposal
              :markdown
                "#{row[3]}"
